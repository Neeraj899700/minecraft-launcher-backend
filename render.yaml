services:
  - type: web
    name: minecraft-backend
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: minecraft-db
          property: connectionString
